# Copilot Instructions for This Repository

## Build, test, and lint commands

No build, test, or lint tooling is configured yet in this repository.

When this project is initialized, infer commands from the first available project manifest (for example: `package.json`, `pyproject.toml`, `Makefile`, `go.mod`, or `Cargo.toml`) and update this file with:

- full build/test/lint commands
- command(s) for running a single test

## High-level architecture

This repository is currently an empty scaffold with no application, library, or infrastructure code checked in yet.

There are no runtime components, modules, or service boundaries to map at this time.

## Key conventions

No project-specific coding conventions or workflow conventions are documented yet (`README.md`, `CONTRIBUTING.md`, and other assistant rule files are not present).

As conventions are introduced, prefer updating this file with concrete repository-specific patterns rather than generic guidance.
