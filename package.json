{
  "name": "timestat",
  "private": true,
  "description": "Dev-time only tooling. No runtime/build dependency on the server: static/app.css is the committed concatenation of tailwind.css + style.css - run 'npm run css' after changing the Tailwind input or style.css.",
  "scripts": {
    "css": "npm run css:tailwind && npm run css:merge",
    "css:tailwind": "tailwindcss -i ./static/input.css -o ./static/tailwind.css --minify",
    "css:merge": "cat ./static/tailwind.css ./static/style.css > ./static/app.css",
    "css:watch": "tailwindcss -i ./static/input.css -o ./static/tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
